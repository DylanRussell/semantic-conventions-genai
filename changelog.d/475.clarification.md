Add guidance on using language-defined context keys across layered libraries to coordinate inference span and event creation, putting events into context on the request path for downstream modification, and preventing duplicate spans, events, and metrics.
